<div class="flash-notifications">
  <% flash.each do |key, value| %>
   <%= content_tag(:div, value, class: key) %>
  <% end %>
</div>

<p>Title:<%= @book.title %></p>
<p>Body:<%= @book.body %></p>

<span><%= link_to "Edit", edit_book_path %>
| <%= link_to "Back", books_path %></span>
